version: '3.8'

services:
  db:
    image: postgres:15
    container_name: netflix_db
    environment:
      POSTGRES_USER: aniya
      POSTGRES_PASSWORD: root1234
      POSTGRES_DB: netflix_data
    ports:
      - "5432:5432"
    restart: alw